<%= form_with(model: issue_type) do |form| %>
  <% if issue_type.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(issue_type.errors.count, "error") %> prohibited this issue_type from being saved:</h2>

      <ul>
        <% issue_type.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :color, style: "display: block" %>
    <%= form.text_field :color %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
