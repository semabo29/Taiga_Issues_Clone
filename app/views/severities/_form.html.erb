<%= form_with(model: severity) do |form| %>
  <% if severity.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(severity.errors.count, "error") %> prohibited this severity from being saved:</h2>

      <ul>
        <% severity.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :color, style: "display: block" %>
    <%= form.text_field :color %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
